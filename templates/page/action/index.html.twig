{% extends 'page/layout.html.twig' %}

{% block title %}Застрахуй.cz{% endblock %}

{% block body %}
    {% include 'page/components/header.html.twig' %}

    <section id="main-slider">
        <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                <div class="carousel-item active" style="position: relative">
                    <div class="slide-1-text rounded pb-4 text-center pt-4 pl-3 pr-3">
                        <p class="mb-0 font-semi-bold slide-1-text-transform">Оформите старховку у нас по самой выгодной цене и получите приятный бонус</p>
                    </div>
                    <img src="{{ asset('assets/page/img/slide1.png') }}" class="d-block w-100" alt="...">
                </div>
            </div>
        </div>
    </section>
    <section class="pt-5 pb-5">
        <div class="container">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }}">
                        {{ message|trans() }}
                    </div>
                {% endfor %}
            {% endfor %}
            <h1 class="margin-bottom-30 text-center font-semi-bold text-uppercase">{{ 'page.whyWe.mainText'|trans }}</h1>
            <div class="h4 d-flex flex-row align-items-center mb-3">
                <i class="color-green fas fa-check-double mr-3"></i> <p class="mb-0 text-justify">{{ 'page.whyWe.part1'|trans }}</p>
            </div>
            <div class="h4 d-flex flex-row align-items-center mb-3">
                <i class="color-green fas fa-check-double mr-3"></i>
                <p class="mb-0 text-justify">{{ 'page.whyWe.part2'|trans }} <strong class="color-red">(Maxima, Uniqa, pVZP, Ergo)</strong>.</p>
            </div>
            <div class="h4 d-flex flex-row align-items-center mb-3">
                <i class="color-green fas fa-check-double mr-3"></i> <p class=" mb-0 text-justify">{{ 'page.whyWe.part3'|trans }}</p>
            </div>
            <div class="h4 d-flex flex-row align-items-center mb-3">
                <i class="color-green fas fa-check-double mr-3"></i> <p class=" mb-0 text-justify">{{ 'page.whyWe.part4'|trans }}</p>
            </div>
            <div class="h4 d-flex flex-row align-items-center mb-3">
                <i class="color-green fas fa-check-double mr-3"></i> <p class=" mb-0 text-justify">{{ 'page.whyWe.part5'|trans }}</p>
            </div>
            <div class="h4 d-flex flex-row align-items-center">
                <i class="color-green fas fa-check-double mr-3"></i> <p class=" mb-0 text-justify">{{ 'page.whyWe.part6'|trans }}</p>
            </div>
        </div>
    </section>
    <section id="contactUs" class="pt-5 pb-5 background-red">
        <div class="container">
            <h2 class="text-center text-white font-semi-bold mb-4 pt-3 text-uppercase">Получите больше информации по телефону</h2>
            <p class="text-center text-white mb-3">
                Оставьте нам свой телефонный номер и мы позвоним Вам со специальным предложением.
            </p>
            <div class="row d-flex justify-content-center">
                {{ form_start(form) }}
                    <div>
                        {{ form_errors(form) }}
                    </div>

                    <div class="d-flex flex-row let-phone">
                        <div class="rotate mr-4">
                            {{ form_row(form.mobile) }}
                        </div>
                        <div class="rotate">
                            {{ form_row(form.save) }}
                        </div>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </section>
    <div class="container">
        <section class="pt-5 pb-5 text-center">
            <h2 class="text-center font-semi-bold mb-5 pt-3 text-uppercase">Узнать больше</h2>

            <div class="row">
                <div class="col-12 col-sm-4">
                    <a class="model-link" href="{{ path('page_our_license') }}">
                        <img src="{{ asset('assets/page/img/order-online.png') }}" alt="order online" class="img-fluid">
                        <p class="mt-4">Проверить лицензию</p>
                    </a>
                </div>
                <div class="col-12 col-sm-4">
                    <a class="model-link" href="{{ path('page_index') }}">
                        <img src="{{ asset('assets/page/img/faq.png') }}" alt="faq" class="img-fluid">
                        <p class="mt-4">Часто задаваемые вопросы</p>
                    </a>
                </div>
                <div class="col-12 col-sm-4">
                    <a class="model-link" href="{{ path('page_contact') }}">
                        <img src="{{ asset('assets/page/img/our-office.png') }}" alt="our office" class="img-fluid">
                        <p class="mt-4">Посетите наш офис</p>
                    </a>
                </div>
            </div>
        </section>
    </div>
    {% include 'page/components/footer.html.twig' %}

{% endblock %}

